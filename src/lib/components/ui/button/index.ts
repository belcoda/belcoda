import Root from './button.svelte';

export { Root, Root as Button };
export { buttonVariants } from './button.svelte';
export type { ButtonProps as Props, ButtonProps, ButtonSize, ButtonVariant } from './button.svelte';
