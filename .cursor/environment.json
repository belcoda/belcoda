{
	"install": "bash .cursor/setup.sh && npx playwright install chromium --with-deps",
	"start": "sudo service postgresql start",
	"terminals": [
		{
			"name": "app-zero",
			"command": "test -f .env || cp .env.example.cloud-agents .env; npm run dev"
		}
	]
}
