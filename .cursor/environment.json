{
	"install": "npm ci && npx playwright install chromium --with-deps",
	"start": "sudo service docker start || true; docker compose up -d --wait",
	"terminals": [
		{
			"name": "app-zero",
			"command": "test -f .env || cp .env.example.cloud-agents .env; npm run db:migrate && npm run db:seed && npm run dev"
		}
	]
}
