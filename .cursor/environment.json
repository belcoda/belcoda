{
	"install": "bash .cursor/setup.sh && npx playwright install chromium --with-deps",
	"start": "sudo service postgresql start 2>/dev/null || service postgresql start",
	"terminals": [
		{
			"name": "app-zero",
			"command": "test -f .env || cp .env.example.cloud-agents .env; PUBLIC_HOST=http://localhost:5173 PUBLIC_ROOT_DOMAIN=localhost:5173 npm run dev"
		}
	]
}
